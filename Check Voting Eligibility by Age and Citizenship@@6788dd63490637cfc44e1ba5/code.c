#include <stdio.h>
int main(){
    int v,c;
    scanf("%d %d",&a,&b);
    if((v>=18)&&(c==1)){
        printf("Eligible");
    }
    else{
        printf("Not Eligible");
    }
    return 0;
}